# 📚 知识库功能完整指南

本指南帮助您快速启用和使用新优化的知识库功能。

---

## ✨ 新增功能概览

您已经获得了一个完全优化的知识库管理系统！包括：

✅ **更显眼的上传按钮** - 使用 "🚀 立即上传" 大按钮
✅ **拖拽上传** - 支持拖拽文件到上传区域
✅ **进度显示** - 实时显示文件处理进度（0% → 100%）
✅ **知识库统计** - 显示知识库中的文档块数
✅ **清空知识库** - 一键清空所有已导入的文档
✅ **RAG 切换开关** - 侧边栏快速启用/禁用知识库检索
✅ **详细使用提示** - 可展开的帮助信息

---

## 🚀 快速启动（5 步）

### 第 1 步：安装 RAG 依赖

```bash
# 进入项目目录
cd RAG

# 激活虚拟环境（如果还没激活的话）
# Windows:
venv\Scripts\activate
# macOS/Linux:
source venv/bin/activate

# 安装 RAG 依赖
pip install -r requirements-rag.txt
```

**注意**：
- 首次安装可能需要 5-10 分钟（下载 ~400MB 的 BGE 嵌入模型）
- 需要足够的磁盘空间（推荐 2GB+）
- 网络需要能访问 HuggingFace（可能需要科学上网）

### 第 2 步：启动应用

```bash
python run.py
```

或者

```bash
streamlit run app/main.py
```

应该看到类似输出：
```
You can now view your Streamlit app in your browser.
Local URL: http://localhost:8501
```

### 第 3 步：在浏览器中打开应用

自动打开或手动访问：`http://localhost:8501`

### 第 4 步：上传文档到知识库

主页面会显示"📚 知识库管理"区域：

```
┌─────────────────────────────────────┐
│ 📚 知识库管理                        │
│ 上传文档到知识库，让 AI 可以从您的... │
│                                     │
│ ┌─────────────────┐  ┌────────────┐ │
│ │ 📤 上传文档      │  │ 📊 知识库  │ │
│ │ 支持PDF和TXT    │  │ 统计      │ │
│ │ [选择文档...]    │  │ 文档块: 0  │ │
│ │ [🚀 立即上传]   │  │ [🗑️清空]  │ │
│ └─────────────────┘  └────────────┘ │
└─────────────────────────────────────┘
```

**上传文档步骤**：
1. 点击或拖拽选择 PDF/TXT 文件
2. 看到文件名显示后，点击 "🚀 立即上传"
3. 等待进度条完成（显示处理百分比）
4. 看到 "✅ 成功导入 X 个文档块" 提示

### 第 5 步：在对话中使用知识库

1. **启用 RAG 检索**：在左侧边栏勾选 "📚 知识库" 中的 "启用 RAG 检索"
2. **提问**：输入您的问题，例如：
   ```
   我上传的文档中讲到了什么？
   告诉我文档中关于 X 的内容
   ```
3. **查看结果**：AI 会自动从知识库检索相关内容，并在回复下显示"📖 相关参考文献"

---

## 📊 用户界面详解

### 主页面 - 知识库管理区域

```
📚 知识库管理
上传文档到知识库，让 AI 可以从您的文档中检索相关信息来提供更准确的答案。

┌─ 上传区域 ──────────────────────────────────────────────┐
│                                                            │
│ ### 📤 上传文档                  ### 📊 知识库统计        │
│ 支持 PDF 和 TXT 格式的文档                                │
│                                                            │
│ [  选择文档 ... ]               📖 文档块: 5               │
│ 📄 已选择: example.pdf (245 KB)  [🗑️ 清空知识库]         │
│                                                            │
│ [🚀 立即上传 .....................]                       │
│                                                            │
└────────────────────────────────────────────────────────────┘

💡 使用提示
点击查看详细说明
  • 支持的文件格式：PDF、TXT
  • 文件处理流程：分块 → 向量化 → 存储
  • 性能建议：单文件 < 50MB，推荐 5-20 个文档
  • RAG 工作原理：搜索 → 注入上下文 → AI 回答
```

### 左侧边栏 - RAG 设置

```
⚙️ 设置

[API 信息区域...]

### 对话参数
[温度滑块...]
[最大令牌数...]

───────────────

### 📚 知识库
[✓ 启用 RAG 检索]

📖 知识库文档: 5
已导入

或者（知识库为空时）：

🔵 启用 RAG 检索: OFF
📭 知识库为空，请上传文档
```

---

## 📝 支持的文件格式

### PDF 文件
- ✅ 自动提取文本
- ✅ 支持多页文档
- ✅ 保留文本结构

**示例**：
```bash
# 上传一份 PDF 报告
报告.pdf  →  自动提取文本  →  分块处理  →  向量存储
```

### TXT 文件
- ✅ 纯文本格式
- ✅ 自动编码检测（UTF-8/GBK）
- ✅ 支持大文件

**示例**：
```bash
# 上传一份 TXT 文档
笔记.txt  →  按换行符分块  →  向量存储
```

---

## ⚙️ 文件处理详解

### 处理流程

```
上传文件
  ↓
[临时保存]
  ↓
[提取文本]
  PDF → PyPDF2 提取
  TXT → 直接读取
  ↓
[分块处理]  (800字符/块，100字符重叠)
  ↓
[向量化]  使用 BGE-Small-zh-v1.5 模型
  每块文本 → 384维向量
  ↓
[存储]  ChromaDB 向量数据库
  持久化保存在 data/chroma_db/
```

### 分块参数

- **块大小**：800 字符
- **重叠**：100 字符（保证上下文连续性）
- **优势**：平衡检索精度和性能

**示例**：
```
原文：
"这是文档的第一部分。这是文档的第二部分。这是文档的第三部分..."

分块结果（800字符/块）：
块1: "这是文档的第一部分。这是文档的第二部分。..."
块2: "这是文档的第二部分。这是文档的第三部分。..."  (100字符重叠)
```

---

## 🔍 RAG 工作原理

当您启用 RAG 并提问时：

```
用户提问：「文档中讲了什么？」
  ↓
[1. 向量化查询]
问题 → BGE 模型 → 384维向量
  ↓
[2. 相似度搜索]
在 ChromaDB 中搜索 Top-3 相似的文档块
相关度排序：高 → 中 → 低
  ↓
[3. 构建增强提示]
系统提示词 + 检索到的文档 + 用户问题
  ↓
[4. AI 生成回复]
发送增强提示 → DeepSeek API → 流式输出
  ↓
[5. 显示结果]
AI 回复 + 📖 相关参考文献 (展示来源和相关度分数)
```

### 示例

**不启用 RAG**：
```
用户：文档中的重点是什么？
AI：我没有访问您的文档，无法告诉您具体内容...
```

**启用 RAG**：
```
用户：文档中的重点是什么？

检索到的相关内容：
• 文档块 1 (相关度 95%)：「本文重点讨论了...」
• 文档块 2 (相关度 87%)：「关键内容包括...」
• 文档块 3 (相关度 82%)：「此外，还提到了...」

AI：根据您上传的文档，主要重点包括：
1. ...
2. ...
3. ...

---
📖 相关参考文献
1. 文档块 1 - 相关度 95%
   「本文重点讨论了...」
```

---

## 🎯 最佳实践

### ✅ 推荐做法

1. **文档质量**
   - 使用清晰、结构化的文档
   - 避免扫描图片（无法提取文本）
   - 使用中英文混合时要清晰

2. **知识库管理**
   - 每个知识库存储相关主题文档
   - 定期清空无关文档
   - 使用有意义的文件名便于管理

3. **提问方式**
   - 具体问题比笼统问题效果好
   - 示例：「文档中提到了哪些具体的案例？」
   - 避免：「文档说了什么？」

4. **性能优化**
   - 单个知识库推荐 5-20 个文档
   - 文件大小：单个 < 50MB
   - 定期验证检索质量

### ❌ 避免做法

- ❌ 上传超大文件（> 100MB）
- ❌ 上传已损坏或乱码 PDF
- ❌ 混合完全无关的文档
- ❌ 每次都上传重复文档（先清空再上传）

---

## 🐛 常见问题

### Q: 上传按钮看不见？
**A**:
1. 确保安装了 RAG 依赖：`pip install -r requirements-rag.txt`
2. 重启应用：`Ctrl+C` 后再运行 `python run.py`
3. 检查浏览器是否加载完整（刷新页面）

### Q: 上传很慢？
**A**: 这是正常的。首次使用时：
- 模型下载：~400MB（只需一次）
- 向量化：大文件需要时间
- 进度条会显示处理进度

### Q: 上传成功但检索不到内容？
**A**: 尝试以下步骤：
1. 确保在侧边栏启用了 "启用 RAG 检索"
2. 提问时使用关键词匹配文档内容
3. 查看侧边栏是否显示文档块数 > 0

### Q: 如何删除特定文档？
**A**: 目前只支持清空整个知识库，步骤：
1. 点击知识库管理区的 "🗑️ 清空知识库"
2. 重新上传需要的文档

### Q: 知识库数据保存在哪里？
**A**: 向量数据存储在 `data/chroma_db/` 目录（持久化保存）

### Q: 支持多少个文档？
**A**: 理论上无限，但推荐：
- 单个知识库：5-20 个文档（性能最佳）
- 单个文件：< 50MB

### Q: 可以同时使用多个知识库吗？
**A**: 当前版本只有一个全局知识库。多知识库功能在计划中。

---

## 📈 性能参数调优

### ChromaDB 设置（可选高级配置）

在 `app/config.py` 中添加：
```python
# ChromaDB 路径（默认: ./data/chroma_db）
CHROMA_DB_PATH = "./data/chroma_db"

# 向量模型
BGE_MODEL_NAME = "BAAI/bge-small-zh-v1.5"

# 分块参数
CHUNK_SIZE = 800          # 字符
CHUNK_OVERLAP = 100       # 字符
```

### 检索参数（在对话时）

```python
# rag_retriever.retrieve_and_build_prompt(query, top_k=3)
# top_k: 检索最相似的几个文档块
# 值越大 → 上下文越多 → Token 消耗越多
# 推荐值: 3-5
```

---

## 📚 完整使用示例

### 场景 1：学习资料库

```
1. 上传课程讲义 PDF
   → 自动分块分散在知识库中

2. 提问：「讲义中关于第三章的核心概念是什么？」
   → AI 检索相关内容并总结

3. 提问：「能举个实际例子吗？」
   → AI 从讲义中查找例子
```

### 场景 2：公司文档库

```
1. 上传：员工手册.pdf
        产品规格.pdf
        工作流程.txt

2. 提问：「新员工入职需要什么流程？」
   → AI 从员工手册检索并回答

3. 提问：「产品的主要特性有哪些？」
   → AI 从产品规格查找并列举
```

### 场景 3：研究论文

```
1. 上传多篇相关领域的论文 PDF

2. 提问：「这些论文的主要发现是什么？」
   → AI 综合多个文档来回答

3. 提问：「与传统方法相比有什么改进？」
   → AI 对比相关内容
```

---

## 🔧 故障排除

### 问题：导入时显示"文档处理器未初始化"

**解决**：
```bash
# 重新安装依赖
pip install -r requirements-rag.txt --force-reinstall

# 重启应用
# Ctrl+C 停止，然后：
python run.py
```

### 问题：Chrome DB 损坏

**解决**：
```bash
# 删除向量数据库（会清空所有知识库）
rm -r data/chroma_db/

# Windows:
rmdir /s data\chroma_db

# 重启应用
python run.py
```

### 问题：内存占用过高

**原因**：
- BGE 模型加载在内存（需要 2GB+）
- ChromaDB 索引加载

**解决**：
- 清空不需要的知识库
- 重启应用

---

## 🚀 下一步计划

- [ ] 多知识库支持（为不同话题创建独立知识库）
- [ ] 知识库管理 UI（编辑、删除、搜索具体文档）
- [ ] 支持更多格式（Word、PowerPoint、Markdown）
- [ ] 批量上传
- [ ] 知识库导出和备份
- [ ] Web 搜索集成（离线使用其他知识库）

---

## 💡 提示

- **第一次启动很慢？** - BGE 模型需要下载和初始化（约 5-10 分钟）
- **没有网络？** - 模型一次下载后会缓存，离线可用
- **想重新训练？** - ChromaDB 是向量数据库，可以不断添加新文档

---

## 📞 获取帮助

如果遇到问题：

1. **查看应用日志**：控制台会显示详细错误信息
2. **运行诊断**：`python test_rag.py`
3. **查阅文档**：
   - [README.md](README.md) - 完整文档
   - [USAGE_EXAMPLES.md](USAGE_EXAMPLES.md) - 使用示例
4. **重置环境**：删除 `data/` 目录重新开始

---

祝您使用愉快！🎉

如有问题或建议，欢迎反馈！

