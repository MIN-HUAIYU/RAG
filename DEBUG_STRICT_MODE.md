# 🐛 严格模式调试指南

## 问题：AI 使用了预训练知识而非知识库

### 症状
- AI 回答了知识库中不存在的内容
- AI 没有引用文献编号
- 回答包含"据我所知"、"根据我的了解"等表述

---

## 🔍 诊断步骤

### 步骤 1：检查 RAG 是否启用

在浏览器界面左侧边栏查看：
- [ ] "启用 RAG 检索" - 必须勾选
- [ ] "🔒 严格模式（仅知识库）" - 必须勾选

如果没有勾选，请勾选后刷新页面。

---

### 步骤 2：查看日志文件

**Windows**:
```bash
type logs\app.log | findstr "RAG"
```

**Linux/Mac**:
```bash
tail -f logs/app.log | grep "RAG"
```

**期望看到的日志**:
```
INFO - === RAG 状态检查 ===
INFO - enable_rag: True
INFO - strict_mode: True
INFO - ✅ RAG 检索已启用，开始检索流程
INFO - 知识库文档数: 31
INFO - 开始检索: 查询='李木田是谁', top_k=3, strict_mode=True
INFO - ✅ RAG 检索完成: 获得 3 个相关文档 (strict_mode=True)
INFO - [1] 小说.txt - 相关度: 85.3% - 内容长度: 800 字符
```

**如果没有看到这些日志，说明 RAG 没有启用！**

---

### 步骤 3：检查知识库是否为空

在浏览器界面左侧边栏查看：
- 📖 知识库文档：应显示数字（如 31）

如果显示 0 或"知识库为空"：
1. 点击顶部"📚 知识库管理"
2. 上传 PDF 或 TXT 文件
3. 等待"✅ 成功导入"提示

---

### 步骤 4：检查文档是否被检索到

提问后，在 AI 回复下方查看：
- [ ] 应该显示"✅ 已从知识库检索到 X 个相关文档"
- [ ] 应该显示"📖 查看参考文献详情"可展开区域
- [ ] 展开后应显示文档来源和相关度

**如果没有显示这些，说明检索失败！**

---

## 🔧 解决方案

### 解决方案 1：刷新页面

最简单的方法：
1. 在浏览器中按 **Ctrl+Shift+R**（强制刷新）
2. 等待页面重新加载
3. 检查侧边栏设置是否正确

---

### 解决方案 2：重启应用

```bash
# 停止应用（按 Ctrl+C）

# 重新启动
python run.py
```

---

### 解决方案 3：清除浏览器缓存

1. 打开浏览器开发者工具（F12）
2. 右键点击刷新按钮
3. 选择"清空缓存并硬性重新加载"

---

### 解决方案 4：检查代码版本

确保使用的是最新版本的代码：

```bash
# 检查 retriever.py 是否包含严格模式
grep "严格模式" rag/retriever.py

# 应该看到输出包含 "【🔒 严格模式 - 必须遵守】"
```

如果没有，说明代码未更新。请重新拉取代码或手动更新文件。

---

### 解决方案 5：手动测试 RAG 检索

运行测试脚本验证 RAG 是否正常工作：

```bash
python test_strict_mode.py
```

期望输出：
```
✅ 测试通过
✅ 严格模式下，系统会明确指示 AI 只使用知识库内容
✅ 当检索不到文档时，Prompt 会要求 AI 回答'没有找到相关信息'
```

---

## 📊 查看实际发送给 DeepSeek 的 Prompt

### 方法 1：在代码中添加调试日志

编辑 `app/main.py`，在第 474 行后添加：

```python
logger.info(f"✅ RAG 检索完成: 获得 {len(retrieved_docs)} 个相关文档 (strict_mode={strict_mode})")

# 添加这行，打印实际的 Prompt
logger.info(f"📝 实际 Prompt:\n{final_prompt}")
```

重启应用后，查看日志文件中的 Prompt 内容。

---

### 方法 2：在侧边栏显示 Prompt（高级）

如果想在界面中看到 Prompt，编辑 `app/main.py`：

```python
# 在第 474 行后添加
if st.session_state.get("show_prompt_debug", False):
    with st.expander("🐛 调试：查看实际 Prompt"):
        st.code(final_prompt, language="markdown")
```

然后在浏览器控制台运行：
```javascript
// 开启 Prompt 显示
window.parent.postMessage({type: 'streamlit:setComponentValue', value: true}, '*')
```

---

## ⚠️ 常见问题

### Q1: 为什么 AI 总是回答"没有找到相关信息"？

**原因**：知识库中确实没有相关内容，或者向量检索质量不高。

**解决方法**：
1. 检查上传的文档是否包含相关内容
2. 尝试换一种问法（使用文档中的关键词）
3. 查看检索日志中的相关度分数（应该 > 50%）

---

### Q2: 检索到了文档，但 AI 还是用预训练知识回答

**原因**：DeepSeek 没有严格遵守系统提示词。

**解决方法**：
1. 确认使用了最新版本的代码（增强的系统提示词）
2. 降低温度参数（左侧边栏 → 温度设为 0.3）
3. 在用户问题前加上提示："请仅根据知识库回答："

---

### Q3: 如何验证 AI 真的使用了知识库？

**验证方法**：
1. ✅ AI 的回答应该引用文献编号（如"根据文献[1]"）
2. ✅ 回答下方应显示"📖 相关参考文献"
3. ✅ 提问知识库外的问题时，应回答"没有找到相关信息"

**测试示例**：
```
# 假设知识库只有公司简介文档

测试 1（知识库内）：公司成立时间是什么？
期望：根据文献[1]，公司成立于...

测试 2（知识库外）：什么是机器学习？
期望：抱歉，我在知识库中没有找到相关信息

如果测试 2 回答了机器学习的定义，说明严格模式未生效！
```

---

## 🚑 紧急处理方案

如果以上方法都不行，执行以下步骤：

### 1. 完全清理并重启

```bash
# Windows
rmdir /S /Q data\chroma_db
del logs\*.log
python run.py

# Linux/Mac
rm -rf data/chroma_db
rm logs/*.log
python run.py
```

### 2. 重新上传文档

1. 启动后，清空知识库（侧边栏 → 🗑️ 清空知识库）
2. 重新上传测试文档
3. 确认显示"✅ 成功导入 X 个文档块"

### 3. 测试简单问题

上传一个包含明确信息的测试文档：

**test.txt**:
```
测试公司名称：ABC科技
成立时间：2025年1月1日
主要产品：测试产品
```

提问："测试公司的成立时间是什么？"

期望回答："根据文献[1]，测试公司的成立时间是2025年1月1日。"

---

## 📞 仍然有问题？

如果以上所有方法都尝试过仍然有问题，请执行：

```bash
# 收集诊断信息
python diagnose_issue.py > diagnosis.txt

# 查看诊断结果
cat diagnosis.txt
```

或者在 GitHub 提交 Issue，附上：
1. `logs/app.log` 的最后 100 行
2. 左侧边栏的设置截图
3. AI 回复的截图
4. 你提的问题和知识库中的内容

---

## ✅ 成功标志

当系统正常工作时，你应该看到：

1. **左侧边栏**：
   - ✅ 启用 RAG 检索
   - ✅ 🔒 严格模式（仅知识库）
   - 📖 知识库文档：31

2. **提问后**：
   - 显示"正在检索知识库..."
   - AI 回复引用文献编号
   - 显示"📖 相关参考文献"

3. **日志中**：
   - `INFO - ✅ RAG 检索已启用，开始检索流程`
   - `INFO - ✅ RAG 检索完成: 获得 3 个相关文档`

4. **测试通过**：
   - 知识库内问题：正确回答并引用
   - 知识库外问题：回答"没有找到相关信息"

---

**祝调试顺利！🎉**
