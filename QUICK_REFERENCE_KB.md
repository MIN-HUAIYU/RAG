# 📚 知识库功能 - 快速参考卡

## 🚀 三步启动

```bash
# 1. 安装依赖
pip install -r requirements-rag.txt

# 2. 启动应用
python run.py

# 3. 打开浏览器
http://localhost:8501
```

---

## 📤 上传文档（主页面）

```
┌─ 📚 知识库管理 ─────────────────────┐
│                                      │
│ ### 📤 上传文档                     │
│ 支持 PDF 和 TXT 格式的文档           │
│                                      │
│ [  选择文档 ... ] ← 点击或拖拽      │
│ 📄 已选择: example.pdf             │
│                                      │
│ [🚀 立即上传] ← 绿色按钮           │
│                                      │
│ 📖 文档块: 5          [🗑️ 清空]    │
│                                      │
└─ 💡 使用提示 ───────────────────────┘
```

---

## 🔍 启用知识库（左侧边栏）

```
⚙️ 设置

### 📚 知识库
┌─────────────────┐
│ ✓ 启用 RAG 检索 │  ← 打开/关闭
└─────────────────┘

📖 知识库文档: 5 已导入
```

---

## 💬 对话时使用

```
用户: 文档中讲了什么？

[AI 思考中...]

📖 相关参考文献 ▼
  1. 文档块 1 (相关度: 95%)
     「本文重点讨论了...」
  2. 文档块 2 (相关度: 87%)
     「此外还提到了...」

AI 回复:
根据您上传的文档，主要内容包括：
1. ...
2. ...
```

---

## 🎯 常用命令

| 任务 | 命令 |
|------|------|
| 安装 RAG 依赖 | `pip install -r requirements-rag.txt` |
| 启动应用 | `python run.py` |
| 测试 RAG | `python test_rag.py` |
| 查看日志 | 查看应用控制台输出 |
| 清空知识库 | 页面上点击"🗑️ 清空知识库" |
| 重置所有 | 删除 `data/chroma_db/` 目录 |

---

## 📊 支持的文件

- ✅ **PDF** - 自动提取文本（最常用）
- ✅ **TXT** - 纯文本文件（最简单）
- ❌ **DOCX** - 暂不支持
- ❌ **Excel** - 暂不支持
- ❌ **图片** - 无法识别（如是扫描件）

---

## ⚙️ 文件处理参数

| 参数 | 值 | 说明 |
|------|-----|------|
| 块大小 | 800 字符 | 单个文档块的字符数 |
| 块重叠 | 100 字符 | 块之间的重叠部分 |
| 检索数量 | 3 个 | 每次对话检索 3 个最相关块 |
| 嵌入模型 | BGE-Small-zh | 中文优化的向量模型 |
| 向量维度 | 384 维 | 向量的特征维度 |

---

## 🐛 快速问题排查

| 问题 | 症状 | 解决方案 |
|------|------|---------|
| 看不到上传 | 页面没有上传按钮 | 检查 RAG 依赖是否装好 |
| 上传失败 | 点击上传后报错 | 查看应用日志，检查文件格式 |
| 检索无结果 | 启用 RAG 但无参考文献 | 确保上传了文档，文件内容清晰 |
| 内存占用高 | 应用卡顿 | 重启应用或清空知识库 |

---

## 💾 数据位置

```
RAG/
├── data/
│   ├── documents/          # 临时文档存储
│   └── chroma_db/         # ⭐ 向量数据库（重要！）
│       ├── chroma-collections.parquet
│       ├── chroma-embeddings.parquet
│       └── chroma-metadata.parquet
└── logs/
    └── app.log            # 应用日志
```

**💡 提示**: 删除 `data/chroma_db/` 会清空所有知识库数据！

---

## 🎓 文件大小参考

| 文件类型 | 推荐大小 | 最大大小 | 处理时间 |
|---------|---------|---------|--------|
| PDF 论文 | < 5MB | < 20MB | 10-30秒 |
| PDF 书籍 | < 20MB | < 50MB | 30-120秒 |
| TXT 笔记 | < 10MB | < 30MB | 5-20秒 |

---

## 📝 输入法（🔤 输入框）

```
左侧边栏底部有一个"🔤"图标，点击切换输入法模式：
• 正常输入：回车发送
• Shift+回车：换行
```

---

## 🔐 隐私和安全

- ✅ 所有数据存储在本地（`data/` 目录）
- ✅ 向量数据不上传到云端
- ✅ API Key 存储在 `.env` 文件中
- ✅ `.gitignore` 自动排除敏感数据

---

## ⚡ 性能优化

### 提速方法
1. **减少文档块数** - 清空不需要的文档
2. **优化提问方式** - 具体问题比笼统问题快
3. **增加 RAG top_k** - 减少检索块数（更快但可能精度降低）
4. **定期重启** - 清理内存占用

### 推荐设置
- 单个知识库：5-20 个文档
- 单个文件：< 50MB
- 温度参数：0.5-0.8（平衡）
- 最大令牌数：2000 个

---

## 🌟 高级技巧

### 1. 快速清空所有
```bash
# 删除向量数据库
rm -r data/chroma_db
# Windows:
rmdir /s data\chroma_db

# 重启应用
python run.py
```

### 2. 导出向量数据（备份）
```bash
# 复制整个 chroma_db 目录
cp -r data/chroma_db data/chroma_db.backup
```

### 3. 检查文档处理
运行测试套件：
```bash
python test_rag.py
```

### 4. 监控日志
在另一个终端运行：
```bash
# 实时查看日志
tail -f logs/app.log  # macOS/Linux

# Windows:
type logs/app.log  # 一次性查看
```

---

## 📚 详细文档

需要更多信息？查看：

- 📖 **KNOWLEDGE_BASE_SETUP.md** - 完整使用指南
- 🚀 **QUICK_START.md** - 初始化步骤
- 📝 **README.md** - 项目概览
- 💡 **USAGE_EXAMPLES.md** - 实际示例
- 🎨 **UI_IMPROVEMENTS_SUMMARY.md** - UI 改进说明

---

## 🆘 紧急求助

1. **应用无法启动？**
   ```bash
   python test_setup.py  # 运行诊断
   ```

2. **依赖安装失败？**
   ```bash
   pip install -r requirements-rag.txt --force-reinstall
   ```

3. **知识库损坏？**
   ```bash
   # 删除损坏的数据库
   rm -r data/chroma_db
   # 重启应用重建
   python run.py
   ```

---

## ✅ 检查清单

启动应用前确保：
- [ ] Python 3.9+ 已安装
- [ ] 虚拟环境已创建
- [ ] `.env` 文件已配置
- [ ] RAG 依赖已安装
- [ ] 磁盘空间 > 2GB
- [ ] 网络连接正常

---

**💡 记住**: 第一次启动可能较慢（模型下载 ~400MB），之后会很快！

---

*最后更新: 2024-12-25*
*需要帮助? 查看 KNOWLEDGE_BASE_SETUP.md*
